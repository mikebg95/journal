spring:
  ai:
    openai:
      api-key: test-key

logging:
  level:
    root: WARN

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

cors:
  allowed-origin-pattern: https://frontend.test